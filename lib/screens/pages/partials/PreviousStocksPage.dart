import 'package:flutter/material.dart';

class PreviousStocksPage extends StatelessWidget {
  const PreviousStocksPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Previous Stocks Page'),
    );
  }
}
